# Aufbereitung der Rohdaten: Extraktion von Texten und Metadaten, Speicherung in einer Datenbank (SQLite?)
# Input: HTML-Dateien (???)
# Output: Strukturierte Daten (JSON, CSV, SQLite)
# Schritte:
# 1. Einlesen der HTML-Dateien
# 2. Extraktion von Texten und Metadaten (z.B. Titel, Datum, Aktenzeichen)
# 3. Speicherung der extrahierten Daten in einer strukturierten Form